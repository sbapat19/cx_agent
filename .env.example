# Copy this file to .env and fill in your values
OPENAI_API_KEY=your_key_here

# Backend (FastAPI)
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend (Vite) - used by start.sh if you customize ports
FRONTEND_PORT=5173

# API URL for frontend (optional; dev proxy uses /api -> backend)
# VITE_API_URL=http://localhost:8000

# Add other env vars as needed (e.g. database, secrets)
# DATABASE_URL=
# SECRET_KEY=
